name: Shell Comands

on: [push]

jobs:
  run-shell-command:
    runs-on: macos-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: ls -lisa
        run: ls -lisa
